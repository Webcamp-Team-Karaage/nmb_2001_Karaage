<!-- ジャンル部分テンプレート -->

<!-- 商品一覧 -->
<h2>商品一覧 (全X件)</h2>
<div>
  <% @products.each do |product| %>
        <div>
          <%= link_to product_path(product.id) do %>
            <%= attachment_image_tag product, :products_image, :
          <% end %>
        </div>
        <div>商品名</div>
        <div>価格</div>
  <% end %>
</div>

