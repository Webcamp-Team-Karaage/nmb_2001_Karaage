<h1>ジャンル一覧・追加画面</h1>
ジャンル　
    <%= form_for(@genre ,url: admin_genres_path) do |f| %>
        <%= f.text_field :name%>
        <%=f.submit %>
    <%end%>


<table border = "1>"
    <% @gerens.each do  |genre|%>
        <tr>
            <td><%= genre.name %></td>
            <td><%= genre.status%></td>
        </tr>
    <%end%>
</table>